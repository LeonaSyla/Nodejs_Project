/* eslint-disable prettier/prettier */
export enum Status {
    COMPLETED = 'completed',
    CANCELLED = 'cancelled',
    TODO = 'to do',
    IN_PROGRESS = 'in progress',
    UNASSIGNED = "unassigned"
}